# HTTP port for this exported workflow service
PORT=8080

# URL of the Connector Hub this workflow should talk to.
# You can point this to:
# - Your own self-hosted connector-hub instance
# - Or the Compose Market-hosted hub: https://connector.compose.market
CONNECTOR_BASE_URL=http://localhost:4001

# Timeout for connector calls (ms)
CONNECTOR_TIMEOUT_MS=60000
